Codex Micro 额度同步工具

1. 先在 macOS 蓝牙设置中完成一次 Codex Micro 配对。
2. 双击“一键额度同步.command”。
3. 首次运行会自动安装本地依赖，请等待完成。
4. 设备未连接时程序只等待，不读取额度；连接后自动读取并写入屏幕。
5. 使用期间保持终端窗口开启，按 Control+C 可停止。

如果 macOS 阻止运行：右键“一键额度同步.command”，选择“打开”。
