Codex Micro for Waveshare ESP32-S3-Touch-LCD-1.85B
V3.0.4 BLE 控制 + USB 麦克风稳定版：2026-08-17

已包含：
- 六个 Agent 状态、切换与颜色显示
- 中央全圆额度与重置倒计时；未连接显示“等待连接”，已连接但无额度显示“未同步额度”
- 发送、快速、同意、拒绝、分支、录音六命令页
- 首页中央短点直接发送当前输入
- BOOT 按住录音和双击免按住录音
- 中央长按分级：0.8 秒进入六快捷键，5 秒进入摇杆，10 秒重新连接蓝牙，20 秒恢复出厂
- 松手选择当前界面模式；快捷页和摇杆页单击中间退出
- 蓝牙 Vendor HID 兼容协议
- 完成提示音和电量读取
- 已连接时额度同步工具
- Companion GUI、命令行和登录自启共用单实例锁，避免争用蓝牙和默认麦克风
- 长按中央 20 秒恢复出厂
- 唯一设备短码和加密额度特征
- 无按钮点击音；仅任务完成、报错、同意提示音与颜色匹配
- 首页使用指定稳定版布局并整体放大：中央额度环、额度数字和外围六键增大，重置时间位于环内
- 屏幕背光同步 Codex Micro 全局 0–100% 亮度，并跟随 Codex 自动调暗与活动恢复
- Agent 蓝/绿/黄/红状态与全局亮度分离，亮度只控制 LCD 背光，不重复缩放颜色
- 自动调暗保留最后的 Agent 状态，唤醒时不会误报任务状态变化
- 新连接的零值初始化报文不会误熄屏；断线时恢复默认可见亮度
- 未插 USB 时可短按硬件 PWR 关机；USB 供电时需先拔线才能真正断电
- 动态标题缺字整行隐藏，不出现问号或方框
- 录音期间隐藏其他 UI，只显示中文计时和真实麦克风波形
- 无主题换色入口，Agent 状态换色和点击反馈仍保留
- 自动旋转已关闭，屏幕保持默认方向
- 中央额度数字调整为 40 像素，底部电池图标与电量百分比完整位于中央圆盘内
- 额度环 50% 以上绿色、20%–49% 黄色、20% 以下红色
- 插线供电/充电并作为 Codex Macro32 Mic，48 kHz、16 位、单声道真实板载麦克风
- USB 只提供麦克风，不提供 CDC、键盘、Vendor HID 或扬声器；所有 Codex 控制继续走蓝牙
- 删除生产固件开机彩条测试，等待屏幕 DMA 完成后再回收绘图缓冲区
- USB 麦克风采集移到 Core 1，并记录复位原因用于后续稳定性确认
- BLE 回调只复制数据入队；JSON、字符串与 NVS 操作移到主循环，避免五分钟额度同步触发 Bluedroid 栈 panic
- 首页 1–6 正确映射到 Codex Desktop 的零基协议槽位 AG00–AG05，第 6 键可切换第 6 个任务
